import React from "react";
import Layout from "@/containers/layout";
import AddClient from "@/containers/client/add-client";

const index = () => {
  return (
    <Layout>
      <AddClient />
    </Layout>
  );
};

export default index;
